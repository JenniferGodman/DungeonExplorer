#include "InputValidation.h"
using namespace std;

/*
* validate the input string and populates the vector result that was also passed.
*/
bool InputValidation::validateInput(string input, vector<validWords>& result) {
	return true;
}